services:
  web:
    build: .
    image: php-multi-cgi-sqlite
    container_name: webapp
    ports:
      - "88:80"
    volumes:
      - ../app:/var/www/html
      # SQLite
      - ../app/data:/var/www/html/data
    restart: unless-stopped
